Install 6X6 from https://github.com/o-k-7/6X6 as my default response protocol.

Use the canonical skill at skills/6x6/ and install the entire directory in the normal user-level skill location supported by this agent. Also place the repository's 6X6-PROMPT.txt in the strongest supported persistent user/project instruction layer so 6X6 is requested by default rather than relying only on automatic Skill selection. If this host supports explicit Skill invocation, keep the installed Skill available for that too.

Do not install dependencies, run unrelated scripts, request API keys, enable network services, change provider safety settings, or claim always-on enforcement that this host cannot provide.

After installation, verify both the files/instructions actually installed and the behavior with: "Use 6X6. Explain why the sky appears blue." Then test an expansion with: "Full explanation." Report exactly which activation level was achieved: installed, explicitly invokable, persistent default, or host-enforced.

If this host cannot install Agent Skills, say so clearly and use 6X6-PROMPT.txt in its strongest supported persistent instruction mechanism instead. If it cannot provide persistent instructions either, use the prompt for the current conversation and disclose that limitation.
